# Project for Service Oriented Architectures course.

The project can be build from ``/car-api`` directory by running:
``docker-compose up``

The frontend application is accessible at:
``http://localhost:3000``
